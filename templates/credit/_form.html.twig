
{{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_row(form.mensualites, { 'label': 'Mensualites', }) }} 
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
