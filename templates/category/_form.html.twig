{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

{{ form_start(form2) }}
    {{ form_widget(form2) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form2) }}
