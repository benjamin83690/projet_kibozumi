{% extends 'base.html.twig' %}

{% block title %}User
{% endblock %}

{% block body %}

	<div class="container">
		<div class="col-md-8">
			<table class="table table-striped">
				<thead class="bg-success">
					<h1>Mon espace</h1>
				</thead>
				<tbody>
					<tr>
						<th>Nom</th>
						<td>{{ user.nom }}</td>
					</tr>
					<tr>
						<th>Prenom</th>
						<td>{{ user.prenom }}</td>
					</tr>
					<tr>
						<th>Email</th>
						<td>{{ user.email }}</td>
					</tr>
					<tr>
						<th>Telephone</th>
						<td>{{ user.telephone }}</td>
					</tr>
				</tbody>
			</table>

			{# <button class="btn btn-primary" href="{{ path('user_index') }}">Retour à la liste des utilisateurs</button> #}

			<button class="btn btn-success" href="{{ path('user_edit', {'id': user.id}) }}">Editer mon profil</button>

			{{ include('user/_delete_form.html.twig') }}
		</div>
	</div>
{% endblock %}
